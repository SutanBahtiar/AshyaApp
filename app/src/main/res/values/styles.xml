<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="buttonStyle" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:layout_margin">@dimen/button_spacing_margin</item>
    </style>

    <style name="buttonStyleCalculation" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:background">@color/colorButtonBackgroundCalculation</item>
        <item name="android:layout_margin">@dimen/button_spacing_margin</item>
    </style>

    <style name="buttonStyleReading" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:background">@color/colorButtonBackgroundReading</item>
        <item name="android:layout_margin">@dimen/button_spacing_margin</item>
    </style>

    <style name="buttonStyleListening" parent="Widget.AppCompat.Button.Colored">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/colorText</item>
        <item name="android:background">@color/colorButtonBackgroundListening</item>
        <item name="android:layout_margin">@dimen/button_spacing_margin</item>
    </style>

</resources>
